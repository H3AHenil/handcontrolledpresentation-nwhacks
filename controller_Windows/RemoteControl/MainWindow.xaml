<Window x:Class="TestOnRemoteControl.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Panel placeholder (currently disabled)
             Future: put buttons/toggles/connection status here.
        -->
        <Grid Grid.Row="0" Visibility="Collapsed" Margin="8" />

        <Grid Grid.Row="1" Background="#333333" 
              IsManipulationEnabled="True"
              ManipulationDelta="OnManipulationDelta"
              ManipulationCompleted="OnManipulationCompleted">

            <TextBlock Text="Swipe in this area to control the mouse\n(pinch = zoom / pan = move)" 
                       Foreground="White" 
                       TextAlignment="Center"
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</Window>
